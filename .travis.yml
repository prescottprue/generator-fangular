language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-fangular' ]; then cd .. && eval "mv $currentfolder generator-fangular" && cd generator-fangular; fi
